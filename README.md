# Blood Donation System (Next.js + TypeScript + Tailwind)

Quick scaffold. To install dependencies and run locally:

```bash
npm install
npm run dev
```

This project uses the `src` folder and the App Router (`src/app`).
